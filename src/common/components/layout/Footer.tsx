export const Footer = () => (
  <footer className="text-mutedForeground mt-24 border-t pt-6 text-xs">
    <div className="flex items-center justify-between">
      <span>shiftroom / {new Date().getFullYear()}</span>
      <span>MIT</span>
    </div>
  </footer>
);
