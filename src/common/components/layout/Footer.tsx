export const Footer = () => (
  <footer className="mt-24 border-t pt-6 font-mono text-xs text-muted-foreground">
    <div className="flex items-center justify-between">
      <span>rsk / {new Date().getFullYear()}</span>
      <span>MIT</span>
    </div>
  </footer>
);
